// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("pcspeak", "for pcspeak support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("pcspeak", "enable pcspeak support", "no");

if (PHP_PCSPEAK != "no") {
	EXTENSION("pcspeak", "pcspeak.c");
}

